<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {# <title>Liste des candidats - {{ center }}</title> #}
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.2;
            margin: 0;
            font-size: 12px;
        }

        .container {
            width: 100%;
            max-width: 680px;
            margin: 0 auto;
        }

        /* Styles for the second table (candidate list) */
        .candidate-table {
            width: 100%;
            max-width: 600px;
            border-collapse: collapse;
            /* Ensures borders are collapsed */
            border: 1px solid black;
            /* Add border to the table */
            margin-top: 10px;
        }

        .candidate-table th,
        .candidate-table td {
            padding: 3px;
            /* Reduced padding */
            vertical-align: top;
            font-size: 13px;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 150px;
            /* Adjust as needed */
            border: 1px solid black;
            /* Add border to cells */
            text-align: center;
            /* Center align text in the cells */
        }

        .presence-column {
            width: 100px;
            /* Adjust as needed */
        }

        .header {
            text-align: center;
        }

        .logo {
            width: 100px;
            height: 100px;
            margin: 0 auto;
        }

        .photo-placeholder {
            width: 100px;
            height: 80px;
            border: 1px solid #000;
            margin: 0 auto;
        }

        .section-title {
            font-weight: bold;
            border-bottom: 1px solid #000;
            margin-top: 5px;
        }

        .footer {
            text-align: center;
            font-style: italic;
            margin-top: 5px;
            font-size: 8px;
        }

        h4 {
            margin: 5px 0;
        }

        p {
            margin: 2px 0;
        }

        .truncate {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 100%;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- First table without borders -->
        <table style="margin-bottom: 5px;">
            <tr>
                <td width="40%" style="text-align: center; font-size: 13px;">
                    <p>UNIVERSITE INTER-ETATS<br>CONGO-CAMEROUN À SANGMELIMA<br>****<br>Ecole Supérieure Internationale
                        de Génie Numérique<br>****</p>
                </td>
                <td width="20%" style="text-align: center;">
                    <img src="{{ absolute_url(asset('/images/uiecc_logo_old1.png')) }}" alt="Logo" class="logo"
                        style="display: block; margin: 0 auto;">
                </td>
                <td width="40%" style="text-align: center; font-size: 13px;">
                    <p>CONGO-CAMEROON INTER-STATES<br>UNIVERSITY AT SANGMELIMA<br>****<br>International Advanced School
                        of Digital Engineering<br>****</p>
                </td>
            </tr>
        </table>

        <div style="text-align: center; margin-bottom:4%;">
            <h4 style="text-decoration: underline; margin: 0; font-size: 13px;">LISTE DES CANDIDATS</h4>
        </div>
        
    {% if field is not null and field is not empty %}
        <div style="text-align: center; margin-bottom:2%;">
            <h4 style="text-decoration: underline; margin: 0; font-size: 12px;">{{ field }}</h4>
        </div>
    {% else %}
        <div style="text-align: center; margin-bottom:2%;">
            <h4 style="text-decoration: underline; margin: 0; font-size: 12px;">{{ candidate.field }}</h4>
        </div>
    {% endif %}            
        
        <div style="text-align: left; margin-bottom:2%;">
            <h4 style="text-decoration: underline; margin: 0; font-size: 12px;">Date: </h4>
        </div>
        <!-- Second table with borders -->
        <table class="candidate-table">
            <thead>
                <tr>
                    <th>N<sup>o<sup> Ordre</th>
                    <th>ID Candidat</th>
                    <th>Nom</th>
                    <th>Sexe</th>
                    <th>Date de naissance</th>
                    {# <th>Domaine d'Etudes</th>
                    <th>Domaine d'Etudes</th> #}
                    <th>Langue de Composition</th>
                    <th class="presence-column">Présence</th>
                </tr>
            </thead>
            <tbody>
                {% for candidate in candidates %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ candidate.candidateID }}</td>
                    <td>{{ candidate.name }}</td>
                    <td>{{ candidate.sexe }}</td>
                    <td>{{ candidate.dateOfBirth|date('d/m/Y') }}</td>
                    {# <td>{{ candidate.field }}</td>
                    <td>{{ candidate.field }}</td> #}
                    <td>{{ candidate.language }}</td>
                    <td></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>

</html>